<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\UavMissionControl.Core\UavMissionControl.Core.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <ItemGroup>
    <!-- Lets App.Tests use MainViewModel's internal (test-seam) constructor. -->
    <InternalsVisibleTo Include="UavMissionControl.App.Tests" />
  </ItemGroup>

</Project>
