<UserControl x:Class="UavMissionControl.App.Views.EventLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="200" d:DesignWidth="600">
    <GroupBox Header="Event Log">
        <ListView x:Name="EventLogListView" ItemsSource="{Binding Entries}" Height="180">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Time" Width="90"
                                    DisplayMemberBinding="{Binding Timestamp, StringFormat={}{0:HH:mm:ss}}"/>
                    <GridViewColumn Header="Severity" Width="70" DisplayMemberBinding="{Binding Severity}"/>
                    <GridViewColumn Header="Message" Width="400" DisplayMemberBinding="{Binding Message}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </GroupBox>
</UserControl>
